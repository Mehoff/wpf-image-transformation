<Window x:Class="ImageTransformation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageTransformation"
        mc:Ignorable="d"
        Title="Image Transformation" Height="450" Width="800">


    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="550"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical">
            <Expander>
                <ScrollViewer>
                    <StackPanel>
                        <!--Name="RadioButtonList"-->
                        <!--ItemsSource="{Binding myCollection}"-->

                        <ListBox x:Name="RadioButtonList" Height="50">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <!--<TextBlock Text="{Binding}"/>-->
                                    <RadioButton GroupName="radioList" Tag="{Binding}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </ScrollViewer>
            </Expander>
            <Expander>
                <ScrollViewer/>
            </Expander>
        </StackPanel>
    </Grid>
</Window>
